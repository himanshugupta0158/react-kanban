import { Status, Priority } from "./utils/data-types";

const statuses: Status[] = ["todo", "in-progress", "done"];

const priorities: Priority[] = ["low", "medium", "high"];

const MAX_POINTS = 45;

export { statuses, priorities, MAX_POINTS };
